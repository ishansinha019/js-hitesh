// basic if else conditions,
// == is used to compare (even if string value is same as int value )values whereas === is used to compare values as well as data types

const month = 3
switch (month) {
    case 1:
        console.log("G2");
        break;
    case 2:
        console.log("D3");
        break;
    case 3:
        console.log("HCF");
        break;
    default:
        console.log("enter a valid strategy number");
        break;
}